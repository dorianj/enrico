import _ from "lodash";
import { DateTime } from "luxon";
import React from 'react';

import { EstimationNode } from './estimation';
import formatters from "./formatters";
import { LUTFact, isLUTFact, isScalarFact, ScalarFact } from './facts';

export type NodeChooserViewProps = {

};

export function NodeChooserView(props: NodeChooserViewProps) {

  return (
    <div className="NodeChooserView">
      Hihi
    </div>
  );
}
